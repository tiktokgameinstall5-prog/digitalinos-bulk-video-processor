PyQt5>=5.15,<6
requests>=2.31,<3

# Auto-captions: faster-whisper bundles ctranslate2 for fast CPU/GPU
# transcription without dragging in the full PyTorch runtime. The chosen
# Whisper model (tiny/base/small) is downloaded on first use into the user's
# AppData via huggingface_hub, so the source distribution stays small.
faster-whisper>=1.0,<2
huggingface_hub>=0.20,<1
